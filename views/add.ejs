<!DOCTYPE html>

<html>
    <head>
        <title>Ajout Metrics</title>
        <link rel="stylesheet" href="/semantic/semantic.min.css">
        <style>
            .main.container{
                margin-top: 10em;
            }
        </style>
    </head>
    <body>
         <div class="ui fixed inverted menu">
             <a href="/" class="header item"> <%= name %></a>
             <button class="ui orange button" href="/logout" onClick='document.location.href="/logout"' type="submit">Déconnexion</button>
         </div>


   <div class="ui main container">
      <h3>Ajouter un nouveau metric : </h3>
      <form action="/metrics" method="POST" class="ui form">
          <div class="field">
              <label for="timestamp">Date</label>
              <input type="datetime-local" name="timestamp"/>
          </div>
          <div class="field">
              <label for="value">Valeur</label>
              <input type="number" name="value"/>
          </div>
          <button class="ui inverted green button" type="submit">Ajouter</button>
          <button class="small ui button" href="/" onClick='document.location.href="/"'>Annuler</button>
      </form>
    </div>
  </body>

  </html>
